<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>💙 for my phaagggliiiiiiiii 💙</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            user-select: none; /* subtle, keeps aesthetic */
        }

        body {
            min-height: 100vh;
            background: radial-gradient(circle at 20% 30%, #0a0f2a, #030617);
            font-family: 'Poppins', 'Segoe UI', system-ui, -apple-system, 'Inter', sans-serif;
            overflow-x: hidden;
            position: relative;
            color: #eef5ff;
        }

        /* canvas particle background */
        #particleCanvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 0;
        }

        /* main containers */
        .content-wrapper {
            position: relative;
            z-index: 10;
            max-width: 1300px;
            margin: 0 auto;
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        /* glassmorphism card */
        .glass-card {
            background: rgba(15, 25, 55, 0.55);
            backdrop-filter: blur(14px);
            border-radius: 2.5rem;
            border: 1px solid rgba(0, 180, 255, 0.3);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3), 0 0 15px rgba(0, 160, 255, 0.2);
            padding: 2.5rem 2rem;
            width: 100%;
            max-width: 600px;
            text-align: center;
            transition: all 0.3s ease;
        }

        .glow-btn {
            background: rgba(0, 140, 255, 0.2);
            border: 1.5px solid #3a86ff;
            backdrop-filter: blur(6px);
            border-radius: 60px;
            padding: 14px 34px;
            font-size: 1.3rem;
            font-weight: 600;
            color: #b9e6ff;
            cursor: pointer;
            transition: 0.25s;
            box-shadow: 0 0 12px #3a86ff80;
            margin-top: 28px;
            letter-spacing: 1px;
        }

        .glow-btn:hover {
            background: #3a86ff30;
            box-shadow: 0 0 22px #3a86ff;
            transform: scale(1.02);
            color: white;
        }

        /* hidden elements */
        .hidden-content {
            display: none;
        }

        /* main reveal area */
        .reveal-area {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 2rem;
        }

        /* huge headline */
        .big-love {
            font-size: clamp(2rem, 10vw, 4.2rem);
            font-weight: 800;
            background: linear-gradient(135deg, #a0e9ff, #3a86ff, #70e0ff);
            background-clip: text;
            -webkit-background-clip: text;
            color: transparent;
            text-align: center;
            text-shadow: 0 0 12px #00a6ff80;
            letter-spacing: -0.5px;
            transform-origin: center;
            margin-bottom: 0.5rem;
            animation: glowPulse 2s infinite ease;
        }

        @keyframes glowPulse {
            0% { text-shadow: 0 0 5px #0099ff; }
            100% { text-shadow: 0 0 22px #3a86ff, 0 0 8px cyan; }
        }

        /* typewriter text */
        .typewriter-text {
            font-size: 1.35rem;
            line-height: 1.6;
            background: rgba(10, 20, 40, 0.6);
            backdrop-filter: blur(6px);
            padding: 1.4rem 1.8rem;
            border-radius: 2rem;
            border-left: 4px solid #3a86ff;
            max-width: 650px;
            text-align: center;
            font-weight: 400;
            margin: 0.5rem 0;
            color: #eaf6ff;
        }

        /* final button */
        .excited-btn {
            background: rgba(0, 150, 255, 0.25);
            border: 1px solid #6eb0ff;
            border-radius: 40px;
            padding: 12px 30px;
            font-weight: 600;
            font-size: 1.1rem;
            margin: 20px 0 40px;
            transition: 0.2s;
            cursor: pointer;
            backdrop-filter: blur(8px);
        }

        .excited-btn:hover {
            background: #3a86ff40;
            transform: scale(1.02);
            box-shadow: 0 0 15px #3a86ff;
        }

        /* MODAL */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            backdrop-filter: blur(12px);
            z-index: 1000;
            display: flex;
            align-items: center;
            justify-content: center;
            visibility: hidden;
            opacity: 0;
            transition: all 0.2s ease;
        }

        .modal-glass {
            background: rgba(10, 20, 45, 0.9);
            backdrop-filter: blur(20px);
            border-radius: 2rem;
            border: 1px solid cyan;
            max-width: 500px;
            width: 85%;
            padding: 2rem;
            text-align: center;
            box-shadow: 0 25px 40px black;
        }

        .timer-box {
            display: flex;
            gap: 1.2rem;
            justify-content: center;
            flex-wrap: wrap;
            margin: 1.8rem 0;
        }

        .time-unit {
            background: #0a142eb3;
            padding: 0.8rem;
            border-radius: 1.5rem;
            min-width: 70px;
            backdrop-filter: blur(4px);
        }

        .time-number {
            font-size: 2.3rem;
            font-weight: 700;
            color: #9ad8ff;
        }

        .close-modal {
            background: #00a3ff30;
            border: none;
            padding: 10px 24px;
            border-radius: 40px;
            margin-top: 1rem;
            cursor: pointer;
            color: white;
        }

        /* floating background items */
        .floating-item {
            position: fixed;
            pointer-events: none;
            z-index: 2;
            will-change: transform;
            filter: drop-shadow(0 0 5px #00a6ff50);
        }

        /* random message bubbles */
        .msg-bubble {
            position: fixed;
            background: rgba(20, 40, 75, 0.85);
            backdrop-filter: blur(12px);
            border-radius: 60px;
            padding: 10px 22px;
            font-size: 0.95rem;
            font-weight: 500;
            color: #caf0ff;
            border: 1px solid #3a86ff;
            box-shadow: 0 5px 14px rgba(0,0,0,0.2);
            z-index: 90;
            white-space: nowrap;
            pointer-events: none;
            animation: bubbleFloat 3s ease-out forwards;
        }

        @keyframes bubbleFloat {
            0% {
                opacity: 0;
                transform: translateY(0px) scale(0.9);
            }
            15% {
                opacity: 1;
                transform: translateY(-10px) scale(1);
            }
            85% {
                opacity: 1;
                transform: translateY(-45px);
            }
            100% {
                opacity: 0;
                transform: translateY(-80px);
            }
        }

        /* shake & flash */
        .shake-effect {
            animation: shakeAnim 0.3s cubic-bezier(0.36, 0.07, 0.19, 0.97) both;
        }

        @keyframes shakeAnim {
            0% { transform: translate(1px, 1px) rotate(0deg); }
            10% { transform: translate(-1px, -2px) rotate(-0.5deg); }
            20% { transform: translate(-2px, 0px) rotate(0.5deg); }
            30% { transform: translate(2px, 1px) rotate(0deg); }
            40% { transform: translate(1px, -1px) rotate(0.5deg); }
            50% { transform: translate(-1px, 2px) rotate(-0.5deg); }
            60% { transform: translate(-2px, 1px) rotate(0deg); }
            70% { transform: translate(2px, 1px) rotate(-0.5deg); }
            80% { transform: translate(-1px, -1px) rotate(0.5deg); }
            90% { transform: translate(1px, 2px) rotate(0deg); }
            100% { transform: translate(0, 0) rotate(0); }
        }

        .flash-overlay {
            position: fixed;
            inset: 0;
            background: white;
            pointer-events: none;
            z-index: 999;
            opacity: 0;
            transition: opacity 0.08s;
        }

        /* explosion items */
        .explode-item {
            position: fixed;
            z-index: 200;
            pointer-events: none;
            width: 45px;
            height: 45px;
            object-fit: contain;
            filter: drop-shadow(0 0 8px cyan);
            will-change: transform, opacity;
        }

        /* subtle decor instead of anime couple */
        .cozy-quote {
            text-align: center;
            font-size: 1.3rem;
            font-style: italic;
            background: rgba(0, 20, 50, 0.5);
            border-radius: 2rem;
            padding: 1rem 1.8rem;
            backdrop-filter: blur(5px);
            margin: 1rem 0;
            border: 1px solid #3a86ff40;
        }

        /* responsiveness */
        @media (max-width: 650px) {
            .glass-card { padding: 1.8rem; }
            .typewriter-text { font-size: 1rem; }
            .big-love { font-size: 2.5rem; }
            .cozy-quote { font-size: 1rem; }
        }
    </style>
</head>
<body>

<canvas id="particleCanvas"></canvas>
<div class="flash-overlay" id="flashOverlay"></div>

<div class="content-wrapper" id="mainWrapper">
    <!-- opening screen card -->
    <div id="openingSection" class="glass-card" style="transition: all 0.4s;">
        <h2 style="font-size: 1.9rem; font-weight: 500; letter-spacing: -0.3px;">phaggglii, do you know one thing?</h2>
        <p style="margin-top: 20px; font-size: 1.2rem; opacity: 0.9;">Click the button to know.</p>
        <button id="revealBtn" class="glow-btn">✨ TAP HERE ✨</button>
    </div>

    <!-- hidden reveal content (shown after click) -->
    <div id="postRevealContent" style="display: none; width: 100%; flex-direction: column; align-items: center;">
        <div class="reveal-area">
            <div id="dynamicHeadline" class="big-love hidden-content" style="display: none;">I LOVEEEEE YOUHHHH SOOO MUCHHHH MIIISHIII 💙</div>
            
            <!-- cozy quote instead of anime couple image -->
            <div id="cozyPlaceholder" class="hidden-content" style="display: none; text-align: center;">
                <div class="cozy-quote">
                    phagggliiii, tumi eneke dukhot piliss nathakbaaa, 
                    i labh yuuu sooo muchhh and tumi eneke dukhi hoi
                    thakile my heart aches the most man... 
                    i love when you do the silly things,
                    pagolor dore sounds ulai thaka mur logot,
                    and enei tumar gaa tu bhal hoi thaka nai
                    aru tate tumi eneke dukhi atma hoi thakile 
                    bohut boya lagta hai jaaannn.. 
                    i loveee yaa sooo muchh and your boobies too 😹😹😘😘😘😘 
                    pilis now smile and stop being sadddd 😽😽😽 <br>

                </div>
            </div>
            
            <div id="messageSection" class="hidden-content" style="display: none;">
                <div id="typewriterPlaceholder" class="typewriter-text"></div>
            </div>
            
            <div id="finalBtnContainer" class="hidden-content" style="display: flex; justify-content: center;">
                <button id="excitedModalBtn" class="excited-btn">💙 Are you excited? 💙</button>
            </div>
        </div>
    </div>
</div>

<!-- modal for countdown -->
<div id="countdownModal" class="modal-overlay">
    <div class="modal-glass">
        <h2 style="margin-bottom: 0.5rem;">✨ Countdown to July 4 ✨</h2>
        <div class="timer-box" id="timerDisplay">
            <div class="time-unit"><div class="time-number" id="days">00</div><div>Days</div></div>
            <div class="time-unit"><div class="time-number" id="hours">00</div><div>Hours</div></div>
            <div class="time-unit"><div class="time-number" id="minutes">00</div><div>Minutes</div></div>
            <div class="time-unit"><div class="time-number" id="seconds">00</div><div>Seconds</div></div>
        </div>
        <p style="margin-top: 6px;">kitnaaaa roi asu xei dintule jetiya
            ami ekelogeee bohut xomoy thakimm no phagglii🥹🥹 
            kitna ki ki korega, bodmasi uuu koregaaa 😹👉👈👉👈
            eagerly waiting for that dayy babessss! 💙</p>
        <button class="close-modal" id="closeModalBtn">Close ✨</button>
    </div>
</div>

<script>
    // ---------- HELPER: placeholder SVG (for missing images) ----------
    function createPlaceholderSVG(label) {
        const svgStr = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100" width="100" height="100"><rect width="100" height="100" fill="#0f1a33" rx="16" stroke="#3a86ff" stroke-width="1.5"/><text x="50" y="55" font-size="12" fill="#b3e4ff" text-anchor="middle" font-family="monospace">${label}</text><text x="50" y="75" font-size="9" fill="#5b8cff" text-anchor="middle">✨</text></svg>`;
        return 'data:image/svg+xml,' + encodeURIComponent(svgStr);
    }

    // fallback for floating images (dietcoke.png, chocofills.png, us.png)
    function setupImageFallbacks() {
        const dietImages = document.querySelectorAll('.dietcoke-img');
        const chocoImages = document.querySelectorAll('.chocofills-img');
        const usImages = document.querySelectorAll('.us-doodle-img');
        dietImages.forEach(img => {
            img.onerror = function() { this.src = createPlaceholderSVG('🥤 Diet Coke'); };
        });
        chocoImages.forEach(img => {
            img.onerror = function() { this.src = createPlaceholderSVG('🍫 Chocofills'); };
        });
        usImages.forEach(img => {
            img.onerror = function() { this.src = createPlaceholderSVG('💙 US'); };
        });
    }

    // ------ global variables ----------
    let musicPlaying = false;
    let audioElement = null;
    let revealDone = false;
    let floatingInterval = null;
    let bubbleInterval = null;
    let timerInterval = null;

    // create background floating elements after reveal
    function startFloatingBackgroundAssets() {
        let container = document.getElementById('floatingBgContainer');
        if(container) container.remove();
        const newContainer = document.createElement('div');
        newContainer.id = 'floatingBgContainer';
        newContainer.style.position = 'fixed';
        newContainer.style.top = '0';
        newContainer.style.left = '0';
        newContainer.style.width = '100%';
        newContainer.style.height = '100%';
        newContainer.style.pointerEvents = 'none';
        newContainer.style.zIndex = '1';
        document.body.appendChild(newContainer);
        
        // generate Diet Coke, Chocofills, US doodle images with correct filenames
        for(let i=0; i<8; i++) createFloatingItem('dietcoke', newContainer);
        for(let i=0; i<7; i++) createFloatingItem('chocofills', newContainer);
        for(let i=0; i<3; i++) createFloatingItem('us', newContainer);
    }

    function createFloatingItem(type, parent) {
        const img = document.createElement('img');
        let className = '';
        let srcFile = '';
        let size = '40px';
        let opacity = 1;
        if(type === 'dietcoke') {
            srcFile = 'dietcoke.png';
            className = 'dietcoke-img';
            size = Math.random() * 30 + 35 + 'px';
            opacity = 0.7;
        } else if(type === 'chocofills') {
            srcFile = 'chocofills.png';
            className = 'chocofills-img';
            size = Math.random() * 28 + 32 + 'px';
            opacity = 0.75;
        } else {
            srcFile = 'us.png';
            className = 'us-doodle-img';
            size = Math.random() * 50 + 65 + 'px';
            opacity = 0.3;
        }
        img.src = srcFile;
        img.classList.add('floating-item', className);
        img.style.width = size;
        img.style.opacity = opacity;
        img.style.position = 'absolute';
        img.style.left = Math.random() * 85 + 5 + '%';
        img.style.top = Math.random() * 90 + 5 + '%';
        const duration = Math.random() * 12 + 10;
        const delay = Math.random() * 8;
        const rotateAni = Math.random() * 360;
        img.style.animation = `gentleFloat ${duration}s infinite ease-in-out`;
        img.style.animationDelay = `${delay}s`;
        img.style.transform = `rotate(${rotateAni}deg)`;
        if(type === 'chocofills') {
            img.style.animation = `floatBounce ${duration}s infinite alternate`;
        }
        parent.appendChild(img);
        img.onerror = () => { img.src = createPlaceholderSVG(type==='dietcoke'?'🥤DC':(type==='chocofills'?'🍫CH':'💙US')); };
    }

    // additional bounce keyframe for chocofills
    const styleSheet = document.createElement("style");
    styleSheet.textContent = `@keyframes gentleFloat { 0% { transform: translateY(0px) rotate(0deg); } 50% { transform: translateY(-12px) rotate(4deg); } 100% { transform: translateY(0px) rotate(0deg); } }
                              @keyframes floatBounce { 0% { transform: translateY(0px) rotate(0deg); } 100% { transform: translateY(-20px) rotate(8deg); } }`;
    document.head.appendChild(styleSheet);

    // ---- explosion burst effect (Diet Coke & Chocofills from center) using correct filenames-----
    function explosionBurst() {
        const centerX = window.innerWidth / 2;
        const centerY = window.innerHeight / 2;
        const burstCount = 26;
        for(let i=0; i<burstCount; i++) {
            const isCoke = i % 2 === 0;
            const img = document.createElement('img');
            img.src = isCoke ? 'dietcoke.png' : 'chocofills.png';
            img.classList.add('explode-item');
            img.style.left = centerX - 25 + 'px';
            img.style.top = centerY - 25 + 'px';
            img.style.width = '50px';
            img.style.position = 'fixed';
            document.body.appendChild(img);
            const angle = Math.random() * Math.PI * 2;
            const velocity = Math.random() * 800 + 500;
            const vx = Math.cos(angle) * velocity;
            const vy = Math.sin(angle) * velocity;
            const rotStart = Math.random() * 360;
            img.style.transform = `rotate(${rotStart}deg)`;
            let start = performance.now();
            function animateExplode(now) {
                const elapsed = (now - start) / 1000;
                if(elapsed > 1.2) {
                    img.remove();
                    return;
                }
                const x = vx * elapsed;
                const y = vy * elapsed - 150 * elapsed * elapsed;
                const rot = rotStart + elapsed * 720;
                img.style.transform = `translate(${x}px, ${y}px) rotate(${rot}deg)`;
                img.style.opacity = 1 - elapsed / 1.2;
                requestAnimationFrame(animateExplode);
            }
            requestAnimationFrame(animateExplode);
            img.onerror = () => { img.src = createPlaceholderSVG(isCoke?'🥤DC':'🍫CH'); };
        }
    }

    // Random Message Bubbles
    function startMessageBubbles() {
        if(bubbleInterval) clearInterval(bubbleInterval);
        const messages = ["loveee yaaaa phagli 💙", "diet cokeeee 😹😹", "smilee piliss 😘"];
        bubbleInterval = setInterval(() => {
            if(!revealDone) return;
            const msg = messages[Math.floor(Math.random() * messages.length)];
            const bubble = document.createElement('div');
            bubble.className = 'msg-bubble';
            bubble.innerText = msg;
            const leftPos = Math.random() * (window.innerWidth - 180);
            const topPos = Math.random() * (window.innerHeight * 0.7) + 20;
            bubble.style.left = `${leftPos}px`;
            bubble.style.top = `${topPos}px`;
            bubble.style.whiteSpace = 'nowrap';
            document.body.appendChild(bubble);
            setTimeout(() => { if(bubble) bubble.remove(); }, 3000);
        }, Math.random() * 4000 + 4000);
    }

    // typewriter effect
    function typewriterEffect(element, text, speed = 45) {
        let i = 0;
        element.innerHTML = '';
        function typing() {
            if(i < text.length) {
                element.innerHTML += text.charAt(i);
                i++;
                setTimeout(typing, speed);
            }
        }
        typing();
    }

    // Countdown to july 4
    function updateCountdown() {
        const targetDate = (() => {
            const now = new Date();
            let year = now.getFullYear();
            let july4 = new Date(year, 6, 4);
            if(now > july4) july4 = new Date(year + 1, 6, 4);
            return july4;
        })();
        function refresh() {
            const now = new Date();
            const diff = targetDate - now;
            if(diff <= 0) {
                document.getElementById('days').innerText = '00';
                document.getElementById('hours').innerText = '00';
                document.getElementById('minutes').innerText = '00';
                document.getElementById('seconds').innerText = '00';
                return;
            }
            const days = Math.floor(diff / (1000 * 60 * 60 * 24));
            const hours = Math.floor((diff % (86400000)) / 3600000);
            const mins = Math.floor((diff % 3600000) / 60000);
            const secs = Math.floor((diff % 60000) / 1000);
            document.getElementById('days').innerText = days < 10 ? '0'+days : days;
            document.getElementById('hours').innerText = hours < 10 ? '0'+hours : hours;
            document.getElementById('minutes').innerText = mins < 10 ? '0'+mins : mins;
            document.getElementById('seconds').innerText = secs < 10 ? '0'+secs : secs;
        }
        refresh();
        if(timerInterval) clearInterval(timerInterval);
        timerInterval = setInterval(refresh, 1000);
    }

    // main reveal function
    function startRevelation() {
        if(revealDone) return;
        revealDone = true;
        // start music
        if(!audioElement) {
            audioElement = new Audio('MUSICHERE.mp3');
            audioElement.loop = true;
            audioElement.volume = 0.5;
        }
        audioElement.play().catch(e => console.log("autoplay blocked but interacted"));
        
        // flash & shake
        const flash = document.getElementById('flashOverlay');
        flash.style.opacity = '0.7';
        setTimeout(() => flash.style.opacity = '0', 150);
        document.body.classList.add('shake-effect');
        setTimeout(() => document.body.classList.remove('shake-effect'), 400);
        
        explosionBurst(); // burst of diet coke & chocofills
        
        // show huge headline with animation
        const headline = document.getElementById('dynamicHeadline');
        headline.style.display = 'block';
        headline.style.animation = 'none';
        headline.offsetHeight;
        headline.style.animation = 'glowPulse 2s infinite, scaleBounce 0.6s cubic-bezier(0.34, 1.3, 0.55, 1)';
        const styleAnim = document.createElement('style');
        styleAnim.textContent = `@keyframes scaleBounce { 0% { transform: scale(0); opacity: 0; } 60% { transform: scale(1.2); } 100% { transform: scale(1); opacity: 1; } }`;
        document.head.appendChild(styleAnim);
        
        // after headline appears, fade in rest of content (no anime couple, instead cozy quote)
        setTimeout(() => {
            document.getElementById('cozyPlaceholder').style.display = 'block';
            document.getElementById('messageSection').style.display = 'block';
            document.getElementById('finalBtnContainer').style.display = 'flex';
            // typewriter message: personal and sweet
            const msgText = "Every single little thing about you makes my world brighter. Your smile heals, your laugh is my favorite melody. I’ll always be here, holding your hand through every storm. You’re my home, my peace, my endless daydream. 💙  (this is just a placeholder for your own sweet words)";
            typewriterEffect(document.getElementById('typewriterPlaceholder'), msgText, 38);
            startFloatingBackgroundAssets();
            startMessageBubbles();
            setupImageFallbacks();
        }, 500);
        
        // remove opening card with fade
        const openingDiv = document.getElementById('openingSection');
        openingDiv.style.transition = 'opacity 0.5s, transform 0.5s';
        openingDiv.style.opacity = '0';
        openingDiv.style.transform = 'scale(0.95)';
        setTimeout(() => {
            openingDiv.style.display = 'none';
            document.getElementById('postRevealContent').style.display = 'flex';
        }, 500);
    }

    // modal logic
    function openModal() {
        const modal = document.getElementById('countdownModal');
        modal.style.visibility = 'visible';
        modal.style.opacity = '1';
        updateCountdown();
    }
    function closeModal() {
        const modal = document.getElementById('countdownModal');
        modal.style.visibility = 'hidden';
        modal.style.opacity = '0';
        if(timerInterval) clearInterval(timerInterval);
    }

    // event listeners
    document.getElementById('revealBtn').addEventListener('click', startRevelation);
    document.getElementById('excitedModalBtn').addEventListener('click', openModal);
    document.getElementById('closeModalBtn').addEventListener('click', closeModal);
    window.addEventListener('click', (e) => {
        const modal = document.getElementById('countdownModal');
        if(e.target === modal) closeModal();
    });

    // background particles (canvas)
    const canvas = document.getElementById('particleCanvas');
    const ctx = canvas.getContext('2d');
    let particles = [];
    function resizeCanvas() {
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;
    }
    function initParticles() {
        const particleCount = 130;
        for(let i=0;i<particleCount;i++) {
            particles.push({
                x: Math.random() * canvas.width,
                y: Math.random() * canvas.height,
                radius: Math.random() * 2.2 + 1,
                alpha: Math.random() * 0.4 + 0.1,
                speedX: (Math.random() - 0.5) * 0.2,
                speedY: (Math.random() - 0.5) * 0.1 + 0.15,
                color: `rgba(100, 180, 255, ${Math.random() * 0.5 + 0.2})`
            });
        }
    }
    function drawParticles() {
        if(!ctx) return;
        ctx.clearRect(0, 0, canvas.width, canvas.height);
        for(let p of particles) {
            ctx.beginPath();
            ctx.arc(p.x, p.y, p.radius, 0, Math.PI*2);
            ctx.fillStyle = p.color;
            ctx.fill();
            p.x += p.speedX;
            p.y += p.speedY;
            if(p.x < 0) p.x = canvas.width;
            if(p.x > canvas.width) p.x = 0;
            if(p.y < 0) p.y = canvas.height;
            if(p.y > canvas.height) p.y = 0;
        }
        requestAnimationFrame(drawParticles);
    }
    window.addEventListener('resize', () => {
        resizeCanvas();
        particles = [];
        initParticles();
    });
    resizeCanvas();
    initParticles();
    drawParticles();
    
    // ensure floating images fallback after load
    window.addEventListener('load', () => {
        setupImageFallbacks();
    });
</script>
</body>
</html>
